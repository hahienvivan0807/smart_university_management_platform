/// Web không có khái niệm "Windows desktop app" theo nghĩa này — luôn dùng
/// `flutter_secure_storage` bình thường (implementation web riêng, không
/// dính bug của Windows desktop).
bool laWindowsDesktop() => false;
